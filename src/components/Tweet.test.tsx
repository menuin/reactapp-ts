import React from "react";
import { render } from "@testing-library/react";
import Tweet from "./Tweet";

describe("Delete button display test", () => {
  const component = render(<Tweet tweetObj={}></Tweet>);
});
